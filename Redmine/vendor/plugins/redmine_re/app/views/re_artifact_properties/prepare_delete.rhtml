<div id='detail_view' class='detail_view'>
<h2> t(:prepare_for_artifact_deletion) </h2>

You asked for deleting the following artifact: <b><%= @artifact.name %></b> (#<%= @artifact.id %>)

<h3><%= t(:re_relations_and_artifacts_found) %></h3>

<p>

<div class="splitcontentleft">

<h4>Relations to other artifacts</h4>
<ul>
  <% for sink in @sinks %>
    <li><%= sink.id.to_s %></li>
  <% end %>
</ul>

<h4>Relations from other artifacts</h4>
<ul>
  <% for source in @sources %>
    <li><%= source.id.to_s %></li>
  <% end %>
</ul>
</div>

<div class="splitcontentright">
<h4>Parent</h4>

<%= @parent.name %><br/>

<h4>Children</h4>
<ul>
  <% for child in @children %>
    <li><%= child.name %></li>
  <% end %>
</ul>

</div>


</p>
<p>
(TODO -> design machen)<br/>
<h3><%= t(:re_choose_option_for_deletion) %></h3>
<ul>
  <li>Delete everything (deletes this artifact<%=', all relations' unless @sources.empty? && @sinks.empty? %><%=' and all children' unless @children.empty? %>)</li>
  <% unless @children.empty? %>
    <li>Delete this artifact and move its children to the parent (<%= @parent.name %></li>
  <% end %>
</ul>
</p>
</div>
