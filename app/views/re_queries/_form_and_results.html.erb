<% if @query.editable? %>
  <%= render 'form', :mode => mode %>
<% end %>

<% if mode == :persist || (mode == :apply && @query.set_by_params?) %>
  <%= render 'results', :mode => mode %>
<% end %>