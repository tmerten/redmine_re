$('#ajax-modal').html('<%= escape_javascript(render :partial => 'export_dialog', :locals => {:project => @project, :artifact => @re_artifact_properties}) %>');
showModal('ajax-modal', '400px');
$('#ajax-modal').addClass('new-comment');