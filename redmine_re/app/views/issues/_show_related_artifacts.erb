<fieldset>
  <legend><%= t(:re_asscociated_requirements) %></legend>
  <% unless @issue.nil? %>
      <%= @issue.re_artifact_properties.collect {
              |properties| link_to properties.name, :controller => properties.artifact_type.underscore, :action => 'edit', :id =>properties.artifact_id
      }.to_sentence %>
  <% end %>
</fieldset>
