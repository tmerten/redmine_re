<% if (re_bb.for_condensed_view or defined? force_representation) and !data.empty? and data[0].value != "" %>
	<td class='one_line'>
		<label class='filter'><%= h(re_bb.name) + ": " %></label>
		<% data.each do |datum| %>		
			<%= h(datum.value.to_s) %><br/>
		<% end %>
	</td>
<% end %>